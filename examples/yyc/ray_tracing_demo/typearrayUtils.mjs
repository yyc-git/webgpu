export let newFloat32Array = (arr) => new Float32Array(arr)

export let newUint8Array = (arr) => new Uint8Array(arr)

export let newUint32Array = (arr) => new Uint32Array(arr)

export let newArrayBuffer = (byteLength) => new ArrayBuffer(byteLength)